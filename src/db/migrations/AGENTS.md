<!-- Parent: ../AGENTS.md -->

# migrations

## Purpose

Drizzle Kit 自动生成的 SQL 迁移文件。不要手动编辑。

## For AI Agents

- `npx drizzle-kit generate` 根据 schema.ts 生成迁移
- `npx drizzle-kit migrate` 执行迁移
- 迁移元数据在 `meta/` 子目录（`_journal.json` + snapshot）
