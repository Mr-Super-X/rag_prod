# RAG 知识库平台 — 功能清单

> 记录 V1 和 V2 全部功能，标注完成状态和技术实现位置。

---

## V1 — 核心平台（已交付）

### 用户系统

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 注册账号 | 完成 | `src/routes/auth.ts` → `auth.service.ts` |
| 登录获取 JWT | 完成 | `src/routes/auth.ts` → `app.jwt.sign()` |
| JWT 认证中间件 | 完成 | `src/middleware/auth.ts` |
| admin/user 角色 | 完成 | `users.role` 字段 + `requireAdmin` 中间件 |
| admin 初始账号 | 完成 | `seedAdmin()`，默认 admin/admin123 |

### 知识库管理

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 创建知识库 | 完成 | `src/routes/kb.ts` → `kb.service.ts` |
| 查看知识库列表 | 完成 | 同上，仅返回自己创建的 |
| 查看知识库详情 | 完成 | 同上 |
| 编辑知识库（仅 owner） | 完成 | 同上 |
| 删除知识库（owner/admin） | 完成 | 同上，级联删除文档+LanceDB 表 |

### 文档管理

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 上传文档（PDF/Word/MD/TXT） | 完成 | `src/routes/doc.ts` → `doc.service.ts` |
| 文件格式校验（白名单） | 完成 | 扩展名 `.pdf|.docx|.md|.txt` |
| 文件大小限制（50MB） | 完成 | `@fastify/multipart` limits |
| 异步处理（上传即返回） | 完成 | `processDocument()` 立即返回 + 后台异步 |
| PDF 文本提取 | 完成 | `pdf-parse` |
| Word 文本提取 | 完成 | `mammoth` |
| MD/TXT 文本读取 | 完成 | `fs.readFile` |
| 文本智能切分（500字+50重叠） | 完成 | `src/pipeline/splitter.ts` |
| 向量化（BGE-m3, 1024D） | 完成 | `src/pipeline/embedder.ts` → Ollama `/api/embed` |
| 向量存储（LanceDB） | 完成 | `src/lib/vectordb.ts` |
| 关键词索引（BM25） | 完成 | `@node-rs/jieba` 分词 + Redis 倒排 |
| 文档列表查看 | 完成 | `src/routes/doc.ts` |
| 删除文档（清向量+BM25+文件） | 完成 | `doc.service.ts` → LanceDB + Redis + fs |
| 文档状态跟踪（处理中/就绪/失败） | 完成 | `documents.status` 字段 |

### 检索与问答

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 向量检索（LanceDB） | 完成 | `searchVectors()` + HNSW 搜索 |
| 关键词检索（BM25） | 完成 | Redis 倒排 + `@node-rs/jieba` |
| RRF 融合排序（k=60） | 完成 | `retrieval.service.ts` → `rrfFuse()` |
| 检索兜底（无结果时提示） | 完成 | `chat.service.ts` → `sources.length === 0` |
| LLM 回答生成（Qwen2.5-7B） | 完成 | `src/pipeline/generator.ts` → Ollama `/api/chat` |
| 引用来源返回（文档名+片段+分数） | 完成 | `ChatResponse.sources` |

### 对话管理

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 创建对话 | 完成 | `context.service.ts` |
| 上下文改写（指代消解） | 完成 | `generator.ts` → `rewriteQuestion()` |
| 多轮对话（6条历史） | 完成 | `getRecentHistory()` |
| 对话列表 | 完成 | `src/routes/chat.ts` |
| 对话详情（含消息） | 完成 | 同上 |
| 删除对话 | 完成 | 同上 |
| 对话标题自动生成 | 完成 | 首条问题截取前50字 |

### 前端

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 登录/注册页面 | 完成 | `LoginPage.vue` / `RegisterPage.vue` |
| 知识库列表+创建弹窗 | 完成 | `KBListPage.vue` |
| 知识库详情（问答+文档管理） | 完成 | `KBDetailPage.vue` |
| 聊天消息列表 | 完成 | `ChatPanel.vue` |
| 消息气泡+引用来源折叠 | 完成 | `MessageBubble.vue` |
| 文件上传按钮 | 完成 | `DocUploader.vue` |
| 文档列表+状态标签 | 完成 | `DocList.vue` |
| 对话历史列表 | 完成 | `ConversationList.vue` |
| 全局顶栏+用户信息 | 完成 | `AppLayout.vue` |
| 登录守卫 | 完成 | `router/index.ts` |
| Loading/Error/Data 三态 | 完成 | `useAsync.ts` |

### 基础设施

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| Docker Compose 部署 | 完成 | `docker-compose.yml`（PG+Redis+Ollama） |
| macOS 原生 Ollama 支持 | 完成 | `docker-compose.mac.yml`（仅 PG+Redis） |
| Swagger API 文档 | 完成 | `http://localhost:3000/docs` |
| 健康检查端点 | 完成 | `GET /api/health` |
| 集成测试（auth/kb/chat） | 完成 | `test/` 目录 |
| 错误分类处理 | 完成 | `src/lib/errors.ts`（AppError 家族） |
| TypeScript 全量类型覆盖 | 完成 | 零 `any`，零编译错误 |

---

## V2 — 增强版本（已交付）

### 流式输出

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| SSE 流式回答 | 完成 | `routes/chat.ts` → `Accept: text/event-stream` |
| Ollama stream 模式 | 完成 | `generator.ts` → `streamGenerate()` |
| 前端逐 token 渲染 | 完成 | `ChatPanel.vue` → `streamChat()` |
| fetch+ReadableStream（非 EventSource） | 完成 | `api.ts` → 支持 POST + Authorization |
| Nginx 反代流式兼容 | 完成 | `X-Accel-Buffering: no` header |

### JWT 安全加固

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| access token（15min 过期） | 完成 | `routes/auth.ts` → `app.jwt.sign({ expiresIn: "15m" })` |
| refresh token（7d 过期） | 完成 | `auth.service.ts` → `generateRefreshToken()` |
| refresh 端点 | 完成 | `POST /api/auth/refresh` |
| refresh rotation（用旧发新+删旧） | 完成 | `refreshAccessToken()` 先删后签 |
| 前端自动无感刷新 | 完成 | `api.ts` → 401 拦截 → refresh → 重试 |
| 并发 refresh 去重 | 完成 | `api.ts` → `refreshPromise` 单例 |
| 密码强度（≥8位+字母+数字） | 完成 | `auth.service.ts` → `validatePassword()` |
| 用户信息持久化（刷新不丢） | 完成 | `auth store` → localStorage |
| login 响应兼容 V1 | 完成 | 同时返回 `token` 和 `accessToken` |

### Reranker 精排

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 检索结果精排 | 完成 | `reranker.ts` + `retrieval.service.ts` |
| LLM 列表式精排 | 完成 | 一次 LLM 调用从候选池中选出 Top-K |
| 候选池扩大（10→25） | 完成 | `RERANKER_TOP_K=25` 当启用 Reranker 时 |
| 降级开关 | 完成 | `RERANKER_ENABLED=false` 时跳过，直接 Top-10 |
| 失败降级 | 完成 | Reranker 异常 → 返回原始候选 |

---

## V3 — 基础设施增强（已交付）

### 备份系统

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| PostgreSQL 自动备份 | 完成 | `scripts/backup.ts` → `pg_dump` via Docker exec |
| LanceDB 备份（tar.gz） | 完成 | 同上 |
| 上传文件备份（tar.gz） | 完成 | 同上 |
| 旧备份自动清理（7天） | 完成 | `cleanOldBackups()` |
| npm script 一键备份 | 完成 | `npm run backup` |

### 结构化日志

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| Pino 双输出（console + 文件） | 完成 | `src/lib/logger.ts` |
| 按天日志文件 | 完成 | `logs/app-YYYY-MM-DD.log` |
| requestId 自动注入 | 完成 | `src/app.ts` → `onRequest` hook |
| 控制台 pretty 格式 | 完成 | `pino-pretty` transport |

### 增量索引

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| chunk SHA256 hash | 完成 | `src/pipeline/splitter.ts` → `sha256()` |
| hash 比对复用未变化 chunk | 完成 | `src/services/doc.service.ts` |
| 变化 chunk 只建新向量 | 完成 | 同上，跳过 hash 匹配的 |
| 失效 chunk 自动清理 | 完成 | `deleteVectors()` 删旧向量 |
| 功能开关 | 完成 | `INCREMENTAL_INDEX=true/false`（默认关） |
| V1 全量模式兼容 | 完成 | `INCREMENTAL_INDEX=false` 时走原逻辑 |

---

---

## V4 — 安全与体验增强（已交付）

### 速率限制

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 全局速率限制 60次/分钟 | 完成 | `src/app.ts` → `@fastify/rate-limit` |
| 超标返回 429 Too Many Requests | 完成 | 插件自动处理 |

### 多租户数据隔离

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| kb_members 表（KB 成员管理） | 完成 | `src/db/schema.ts` |
| requireKBAccess 中间件 | 完成 | `src/middleware/auth.ts` |
| KB 创建者自动成为 owner 成员 | 完成 | `src/services/kb.service.ts` → `createKB()` |
| KB 创建者始终有权限（兼容旧数据） | 完成 | `requireKBAccess` 先查 `createdBy` 再查 `kb_members` |
| 非成员访问返回 403 | 完成 | 中间件拦截 |

### 文档处理进度轮询

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 有处理中文档时每 3 秒自动刷新 | 完成 | `client/src/components/DocList.vue` |
| 全部就绪后自动停止轮询 | 完成 | `onUnmounted` + `hasProcessing` 判断 |

### 思考过程展示

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 发送问题后显示"检索中..." | 完成 | `ChatPanel.vue` → `thinking` ref |
| 脉冲动画指示器 | 完成 | CSS `@keyframes pulse` |
| 首 token 到达后自动消失 | 完成 | `onToken` 回调清空 `thinking` |

---

## 功能对比

| 维度 | V1 | V2 | V3 | V4 |
|------|----|----|----|----|
| 回答生成 | 全文一次性返回 | 逐字流式输出 | — | 思考过程动画 |
| JWT 安全 | 永不过期 | access+refresh | — | — |
| 检索精度 | RRF Top-10 | Reranker Top-5 | — | — |
| 数据库备份 | 无 | 无 | PG+LanceDB+uploads | — |
| 日志 | 终端临时 | 终端临时 | 文件持久化+requestId | — |
| 文档更新 | 全量重建 | 全量重建 | hash 比对增量 | — |
| 速率限制 | 无 | 无 | 无 | 60次/分钟 |
| KB 访问控制 | 知道 ID 就能访问 | 知道 ID 就能访问 | 知道 ID 就能访问 | 仅 owner+成员 |
| 思考过程 | 无 | 无 | 无 | "检索中..."提示 |
| 文档进度 | 手动刷新 | 手动刷新 | 手动刷新 | 自动轮询 |
| 后端源文件 | 29 | 31 | 33 | **33** |
| API 端点 | 14 | 15 | 15 | 15 |
| 数据库表 | 6 | 7 | 7 | **8**（+kb_members） |
| 文档数 | 5 | 6 | 7 | 7 |
---

## V5 — 云端 LLM + 深色模式 + E2E（已交付）

### 云端 LLM 一键切换

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| LLM_PROVIDER=ollama\|deepseek 切换 | 完成 | `config.ts` + `generator.ts` 工厂模式 |
| DeepSeek API 接入 | 完成 | `deepseekChat()` / `deepseekStream()` |
| API 失败自动降级 Ollama | 完成 | try/catch → 回退本地 |
| Embedding 始终本地 | 完成 | `embedder.ts` 不改动 |

### 深色模式 + 移动端

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| CSS 变量暗色主题 | 完成 | `App.vue` → `body.dark {}` |
| ☀/☾ 一键切换 + localStorage 记住 | 完成 | `AppLayout.vue` |
| 768px 断点响应式 | 完成 | `@media (max-width: 768px)` |

### E2E 测试

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| Playwright 自动化测试 | 完成 | `test/e2e/rag-flow.spec.ts` |
| 覆盖：注册→登录→KB→上传→提问 | 完成 | 全流程 5 步验证 |
| npm run test:e2e | 完成 | `package.json` |

---

## V6 — 成员管理 UI（已交付）

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| KB 成员列表（含用户名） | 完成 | `GET /api/kb/:id/members` → JOIN users |
| 通过用户名添加成员 | 完成 | `POST /api/kb/:id/members` |
| 移除成员（不可移 owner）| 完成 | `DELETE /api/kb/:id/members/:userId` |
| 成员管理前端页面 | 完成 | `client/src/components/MemberManager.vue` |
| KB 详情页"成员"标签 | 完成 | `KBDetailPage.vue` |

### Agent 能力

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| LLM 意图检测 | 完成 | `agent.ts` → `detectAndExecute()` |
| 时间查询 | 完成 | `get_current_time` 函数 |
| 计算器 | 完成 | `simple_calculator` 函数 |
| 检索前拦截 | 完成 | `chat.service.ts` → 函数调用跳过检索 |
| 功能开关 | 完成 | `.env` → `AGENT_ENABLED=true` |

---

## 功能对比

| 维度 | V1-V5 | V6 |
|------|-------|----|
| KB 成员管理 | 后端有，前端无 | 完整 UI |
| API 端点 | 15 | **18**（+3 成员接口） |

## V7 — 体验增强（已交付）

| 功能 | 状态 | 改动 |
|------|------|------|
| KB 列表分组（我创建的/共享给我的） | 完成 | `KBListPage.vue` |
| 思考过程分阶段展示 | 完成 | `ChatPanel.vue` |
| Excel(.xlsx) 解析 | 完成 | `parser.ts` + `xlsx` npm 包 |
| PPT(.pptx) 解析 | 完成 | `parser.ts` → ZIP XML 文本提取 |
| 上传白名单扩展 | 完成 | `routes/doc.ts` |

## V8 — 管理员控制台 + 文档进度（已交付）

| 功能 | 状态 | 改动 |
|------|------|------|
| 用户管理（列表+删除） | 完成 | `routes/admin.ts` |
| 知识库管理（列表+强制删除） | 完成 | 同上 |
| 管理页前端（仅 admin 可见） | 完成 | `AdminPage.vue` + `AppLayout.vue` + 路由 |
| 文档进度轮询加速（3s→2s） | 完成 | `DocList.vue` |

## V9 — 对话导出 + 管理员增强（已交付）

| 功能 | 状态 | 改动 |
|------|------|------|
| 对话导出为 Markdown | 完成 | `chat.ts` + `ConversationList.vue` |  
| KB 使用统计（文档数/对话数） | 完成 | `admin.ts` + `AdminPage.vue` |

## V10 — 产品体验打磨（已交付）

### 引用高亮

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| LLM 引用标注指令 | 完成 | `generator.ts` → SYSTEM_PROMPT 追加 [1][2] 标注规则 |
| 引用后处理验证 | 完成 | `chat.service.ts` → `validateAndInjectCitations()` 验证/剥离无效引用 |
| Bigram 兜底注入 | 完成 | 同上 → LLM 未输出引用时，基于 bigram 重叠自动注入 |
| buildContext 格式统一 | 完成 | `[N] filename\ncontent` 格式，三处调用统一 |
| segment 数组解析渲染 | 完成 | `MessageBubble.vue` → `parseContent()` → `v-for` 渲染（非 v-html） |
| 引用编号点击展开/收起 | 完成 | 点击 [N] → 消息下方展开 sources[N-1] 原文，再次点击收起 |
| computed 缓存解析结果 | 完成 | `parsedContent` computed 避免每次重渲染 regex |

### 文档预览

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 预览端点 | 完成 | `GET /api/kb/:id/docs/:docId/preview?offset=0&limit=50` |
| 分页加载 | 完成 | `doc.service.ts` → `getDocPreview()` 按 chunk_index 分页 |
| 就绪状态守卫 | 完成 | status !== 'ready' → 400 + "文档处理中，暂不可预览" |
| 预览弹窗组件 | 完成 | `DocPreviewModal.vue`（新建）→ Loading/Error/Empty 三态 |
| 关闭方式 | 完成 | ✕ 按钮 / 点击遮罩 / ESC 键 |
| 文档列表预览按钮 | 完成 | `DocList.vue` → 就绪文档行显示"预览"按钮 |

### 对话搜索

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 搜索输入框 | 完成 | `ConversationList.vue` → 列表顶部搜索框 |
| 实时标题过滤 | 完成 | `computed` → `filteredConvs` 按标题匹配 |
| 搜索时暂停轮询 | 完成 | `watch(searchKeyword)` → 有关键词时停止 5s 轮询 |
| 清空恢复 | 完成 | 清空搜索框 → 恢复完整列表 + 恢复轮询 |

### 附带修复

| 问题 | 解决 |
|------|------|
| SSE 路由 fallback/agent 重复判断 | `e.type === "fallback"` 出现两次 → 第一个改为 `"agent"` |

### V10 技术指标

| 指标 | 值 |
|------|----|
| 新增文件 | 1（DocPreviewModal.vue） |
| 修改文件 | 9 |
| 新增 API 端点 | 1（文档预览） |
| 新增 npm 依赖 | 0 |
| 编译错误 | 0 |

## V11 — 反馈闭环 + 企业就绪（已交付）

### 反馈系统

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 赞/踩按钮 | 完成 | `MessageBubble.vue` → assistant 消息下方显示 👍👎 |
| 反馈 upsert 切换 | 完成 | `POST /api/messages/:id/feedback` → 同 rating 再点取消 |
| 反馈存储 | 完成 | `message_feedback` 表（messageId/userId UNIQUE 约束） |
| 反馈统计 | 完成 | `GET /api/admin/feedback-stats` → 总评分数/好评率/最近列表 |
| 管理页"反馈概览"标签 | 完成 | `AdminPage.vue` |

### 审计日志

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 审计记录工具 | 完成 | `src/lib/audit.ts` → `logAudit()` fire-and-forget |
| 登录审计 | 完成 | `routes/auth.ts` → login 成功后记录 |
| KB 操作审计 | 完成 | `routes/kb.ts` → createKB/deleteKB |
| 文档操作审计 | 完成 | `routes/doc.ts` → uploadDoc/deleteDoc |
| 审计日志查看 | 完成 | `GET /api/admin/audit-logs` → 管理页"审计日志"标签 |
| IP 记录 | 完成 | x-forwarded-for 解析 + request.ip |

### API Key

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| API Key 生成 | 完成 | `POST /api/auth/api-keys` → HMAC-SHA256 hash 存储，明文仅首次返回 |
| API Key 列表/吊销 | 完成 | `GET/DELETE /api/auth/api-keys/:id` |
| API Key 鉴权 | 完成 | `authenticateApiKey` 中间件 → SHA256 hash 比对 |
| Chat 路由双重鉴权 | 完成 | `routes/chat.ts` → JWT 或 API Key（OR 逻辑） |
| 设置页面 | 完成 | `SettingsPage.vue`（新建）→ 生成/查看/吊销 API Key |
| Logger 遮盖 | 完成 | `logger.ts` → `redact: ["req.headers.authorization"]` |

### 使用仪表盘

| 功能 | 状态 | 实现位置 |
|------|------|---------|
| 使用概览 | 完成 | `GET /api/admin/overview` → 用户/KB/文档/对话/今日提问 |
| 管理页"使用概览"标签 | 完成 | `AdminPage.vue` → 5 张统计卡片 |

### V11 技术指标

| 指标 | 值 |
|------|----|
| 新增文件 | 2（audit.ts + SettingsPage.vue） |
| 修改文件 | 13 |
| 新增 API 端点 | 7 |
| 新增 DB 表 | 3 |
| 新增 npm 依赖 | 0 |
| 编译错误 | 0 |
| 安全审查 | SHA256→HMAC-SHA256 修复 2 CRITICAL |

---

| 维度 | V1 | V2 | V3 | V4 | V5 |
|------|----|----|----|----|----|
| LLM 推理 | 本地 CPU | 本地 CPU | — | — | 云端秒回 or 本地 |
| 深色模式 | 无 | 无 | 无 | 无 | ☀/☾ 一键切换 |
| 移动端 | 仅桌面 | 仅桌面 | 仅桌面 | 仅桌面 | 768px 响应式 |
| E2E 测试 | 无 | 无 | 无 | 无 | Playwright 全流程 |
| 后端源文件 | 29 | 31 | 33 | 33 | 33 |
| 编译错误 | 0 | 0 | 0 | 0 | 0 |
| 文档数 | 5 | 6 | 7 | 7 | 7 |

| 纬度 | V6 | V7 | V8 | V9 | V10 | V11 | V12 | V13 | V14 |
|------|----|----|----|----|-----|-----|-----|-----|
| 成员管理 | 完整 UI | — | — | — | — | — | — | — |
| Agent 能力 | 时间+计算器 | — | — | — | — | — | — | — |
| KB 列表分组 | 无 | 我创建的/共享的 | — | — | — | — | — | — |
| Excel/PPT 解析 | 无 | xlsx+pptx | — | — | — | — | — | — |
| 管理员控制台 | 无 | 无 | 用户+KB 管理 | — | — | — | — | — |
| 对话导出 | 无 | 无 | 无 | Markdown | — | — | — | — |
| KB 统计 | 无 | 无 | 无 | 文档数+对话数 | — | — | — | — |
| 引用高亮 | 无 | 无 | 无 | 无 | [N] 点击展开 | — | — | — |
| 文档预览 | 无 | 无 | 无 | 无 | 弹窗分页预览 | — | — | — |
| 对话搜索 | 无 | 无 | 无 | 无 | 标题实时过滤 | — | — | — |
| 赞/踩反馈 | 无 | 无 | 无 | 无 | 无 | 👍👎 + 统计 | — | — |
| 审计日志 | 无 | 无 | 无 | 无 | 无 | 5 操作点记录 | — | — |
| API Key | 无 | 无 | 无 | 无 | 无 | HMAC-SHA256 | — | — |
| 使用仪表盘 | 无 | 无 | 无 | 无 | 无 | 5 项统计卡片 | — | — |
| 使用趋势图 | 无 | 无 | 无 | 无 | 无 | 无 | ECharts 30天 | — |
| 置信度+进度 | 无 | 无 | 无 | 无 | 无 | 无 | 三色+5步 | — |
| AI引擎配置 | 无 | 无 | 无 | 无 | 无 | 无 | 无 | Embedding+LLM统一面板 | — |
| 向量迁移 | 无 | 无 | 无 | 无 | 无 | 无 | 无 | 批量+版本切换+回滚 | — |
| 精排+缓存 | 无 | 无 | 无 | 无 | 无 | 无 | 无 | 无 | Cross-Encoder+Redis24h |
| 后端源文件 | 34 | 34 | 36 | 36 | 36 | 30 | 33 | 35 | 36 |
| 前端源文件 | 28 | 28 | 29 | 29 | 30 | 21 | 25 | 27 | 28 |
| API 端点 | 18 | 18 | 22 | 22 | 23 | 32 | 36 | 42 | 43 |
| 编译错误 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 踩坑总数 | 23 | 23 | 25 | 27 | 27 | 38 | 41 | 43 | 46 |
| 数据库表 | 8 | 8 | 8 | 8 | 8 | 11 |
